// const express = require('express');
// const routes = require('./routes');
// require('dotenv').config();
// const app = express();

// const HTTP_PORT = parseInt(process.env.PORTS2 || 8080, 10);

// app.use('/v1', routes);

// app.listen(HTTP_PORT, () => {
//     console.info('Server: ' + HTTP_PORT);
// });
